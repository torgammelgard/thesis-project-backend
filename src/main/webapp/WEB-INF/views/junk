		
		<!-- backup -->
		<h1>Add a match page!</h1>

		<!--/*@thymesVar id="match" type="se.torgammelgard.persistence.entities.Match"*/-->
		<form class="form-horizontal" action="#" th:action="@{/match/save}"
			th:object="${match}" method="post">

			<div class="form-group">
				<label for="name" th:text="#{addmatchform.name}">--Match name--</label> <input
					id="name" class="form-control" type="text" th:field="*{name}"/>
			</div>
			
			<!--/*@thymesVar id="team" type="se.torgammelgard.persistence.entities.Team"*/-->
			<div class="panel panel-default col-xs-6">
				<div class="form-group">
					<label for="teamoneselect" th:text="#{addmatchform.teamone}"></label>
					<select id="teamoneselect" class="form-control"
						th:field="*{teamOne}">
						<option th:each="team : ${teams}" th:value="${{team}}"
							th:text="${team.getTeamName()}"></option>
					</select>
				</div>
				<div class="form-group">
					<label for="teamtwoselect" th:text="#{addmatchform.teamtwo}"></label>
					<select id="teamtwoselect" class="form-control"
						th:field="*{teamTwo}">
						<option th:each="team : ${teams}" th:value="${{team}}"
							th:text="${team.getTeamName()}"></option>
					</select>
				</div>
			</div>
			
			<div class="panel panel-default col-xs-6">
				<table class="table">
	
					<thead>
						<tr>
							<th th:each="tennisSet : ${match.tennisSets}" th:text="${tennisSet.setNumber}"></th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<th:block th:each="tennisSet : ${match.tennisSets}">
								<td th:each="tennisSetScore : ${tennisSet.tennisSetScore}">
									<input class="form-control" type="text" th:value="${tennisSetScore.scoreTeamOne}">
								</td>
							</th:block>
						</tr>
						<tr>
							<th:block th:each="tennisSet : ${match.tennisSets}">
								<td th:each="tennisSetScore : ${tennisSet.tennisSetScore}">
									<input class="form-control" type="text" th:value="${tennisSetScore.scoreTeamTwo}">
								</td>
							</th:block>
						</tr>
					</tbody>
	
				</table>
			</div>
			
			<button type="submit" class="btn btn-default">Submit</button>
		</form>